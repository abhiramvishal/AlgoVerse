import { createPlaceholderModule } from "@/visualizations/placeholder";

export const dfsModule = createPlaceholderModule(
  "graph-dfs",
  "dfs",
  "Depth-First Search",
  ["algorithms", "graph"],
);
